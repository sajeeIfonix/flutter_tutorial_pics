// Import a helper library from flutter to get content on screen
import 'package:flutter/material.dart';

import 'src/app.dart';

// Define a 'main' function to run when our app starts
void main() {
// Render text widget on screen
  runApp(const App());
}
